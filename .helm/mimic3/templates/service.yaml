apiVersion: v1
kind: Service
metadata:
  name: {{ include "mimic3.fullname" . }}
  labels:
    {{- include "mimic3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "mimic3.selectorLabels" . | nindent 4 }}
